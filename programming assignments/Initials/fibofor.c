#include <stdio.h>
int main()
{
    int i, fib;
    for (i = 0, fib = 0; i < 2; i++)
    {
        (fib = fib + i);
        printf("%d \n", fib);
    }
    // for (i = 1; i < 10; i++)
        
    // printf("")
}
