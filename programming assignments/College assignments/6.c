#include <stdio.h>
int main()
{
    int X = 20;
    {
        int X = 40;
        // float a, b, r;
        // printf("Enter the first number \n");
        // scanf("%f", &a);
        // printf("Enter the second number \n");
        // scanf("%f", &b);
        // r = (a + b);
        // printf("The sum of entered numbers is %f \n", r);
        printf("%d\n", X);
    }
    printf("%d\n", X);
}
